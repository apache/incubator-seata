language: java
sudo: false # faster builds

jdk:
  - oraclejdk11
  - oraclejdk8
  - openjdk7

cache:
  directories:
    - $HOME/.m2

install: true

script:
  - mvn clean cobertura:cobertura package

after_success:
  - bash <(curl -s https://codecov.io/bash)